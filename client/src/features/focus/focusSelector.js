export const selectFocusSession = (state) => state.focus.session;

export const selectFocusStatus = (state) => state.focus.status;

export const selectFocusTimeLeft = (state) => state.focus.timeLeft;

export const selectNextFocusSession = (state) => state.focus.nextSession;

export const selectFocusSettings = (state) => state.focus.settings;

export const selectLastEndedFocusSegment = (state) => state.focus.lastEndedSegment;
